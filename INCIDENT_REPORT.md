# Incident Report — Auto-Rollback Executed

**Date:** 2026-05-08T06:29:00.462Z
**Alarm:** demo-5xx-ApiGw5xxAlarm

## What Happened

A CloudWatch 5xx alarm (`demo-5xx-ApiGw5xxAlarm`) fired shortly after a new Lambda deployment was promoted to the `live` alias. Aziron Post-Deployment Validation Agent was triggered and executed a full validation sweep — HTTP health checks, CloudWatch metric analysis, and integration tests — against the API Gateway endpoints. The `/checkout` endpoint returned HTTP 500 errors consistently across all integration test runs, while `/health` and `/products` remained healthy. The 5xx error rate exceeded the acceptable threshold, resulting in a **NO-GO** verdict.

## Root Cause

The root cause was identified as a broken integration between the checkout Lambda handler and the `cart_service` module. Specifically, the newly deployed version introduced a call to `cart_service.get_cart()` that failed at runtime due to an incompatible interface change — the function signature was updated in `cart_service` but the checkout handler was not updated to match. This caused an unhandled exception on every `/checkout` request, producing HTTP 500 responses from API Gateway.

## Auto-Remediation

Aziron detected the failure and automatically rolled back the Lambda alias `live` to the stable version.
Post-rollback verification: PASSED — all endpoints restored to 200 OK.

## Required Fix Before Re-Deploying

- Fix the `cart_service.get_cart()` integration in the checkout handler
- Ensure all 3 integration tests pass locally before deploying
- Deploy with: `./infra/deploy.sh deploy`

---
*Auto-generated by Aziron Post-Deployment Validation Flow*
