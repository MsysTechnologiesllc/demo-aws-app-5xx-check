# Incident Report — Auto-Rollback Executed

**Date:** 2026-06-04T06:43:33.079Z
**Alarm:** demo-5xx-ApiGw5xxAlarm

## What Happened

A deployment triggered CloudWatch alarm `demo-5xx-ApiGw5xxAlarm` due to elevated 5xx error rates on the API Gateway. Post-deployment health checks and integration tests confirmed that one or more endpoints were returning HTTP 500 errors, indicating a regression introduced by the new deployment.

## Root Cause

The `POST /checkout` endpoint began returning HTTP 500 errors immediately after deployment. Integration test analysis identified a broken integration between the checkout handler and `cart_service.get_cart()`. The new deployment introduced a failure in this call path, causing unhandled exceptions in the checkout Lambda function and resulting in 5xx responses propagated through API Gateway.

## Auto-Remediation

Aziron detected the failure and automatically rolled back the Lambda alias `live` to the stable version.
Post-rollback verification: PASSED — all endpoints restored to 200 OK.

## Required Fix Before Re-Deploying

- Fix the `cart_service.get_cart()` integration in the checkout handler
- Ensure all 3 integration tests pass locally before deploying
- Deploy with: `./infra/deploy.sh deploy`

---
*Auto-generated by Aziron Post-Deployment Validation Flow*
