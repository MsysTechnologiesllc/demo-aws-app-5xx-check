# Incident Report — Auto-Rollback Executed

**Date:** 2026-05-15T04:33:26.371Z
**Alarm:** demo-5xx-ApiGw5xxAlarm

## What Happened

A deployment was pushed to the Lambda function and promoted to the `live` alias. Shortly after promotion, the CloudWatch alarm `demo-5xx-ApiGw5xxAlarm` fired, indicating a spike in HTTP 5xx errors on the API Gateway. The Aziron Post-Deployment Validation Agent was triggered and performed health checks, CloudWatch metric analysis, and integration tests against all registered endpoints. The `/checkout` endpoint returned HTTP 500 responses consistently, while `/health` and `/products` remained healthy. Integration tests confirmed a failure in the checkout flow, resulting in a NO-GO verdict.

## Root Cause

The newly deployed Lambda version introduced a broken integration with `cart_service.get_cart()` inside the checkout handler. The function call fails at runtime due to an incompatible response schema returned by the cart service, causing an unhandled exception and a 500 Internal Server Error on every `POST /checkout` request.

## Auto-Remediation

Aziron detected the failure and automatically rolled back the Lambda alias `live` to the stable version.
Post-rollback verification: PASSED — all endpoints restored to 200 OK.

## Required Fix Before Re-Deploying

- Fix the cart_service.get_cart() integration in the checkout handler
- Ensure all 3 integration tests pass locally before deploying
- Deploy with: ./infra/deploy.sh deploy

---
*Auto-generated by Aziron Post-Deployment Validation Flow*
