# Incident Report — Auto-Rollback Executed

**Date:** 2026-06-22T12:17:46.220Z
**Alarm:** demo-5xx-ApiGw5xxAlarm

## What Happened

A CloudWatch 5xx alarm fired shortly after a new Lambda deployment was promoted to the `live` alias. Aziron Post-Deployment Validation Agent was triggered and executed a full validation suite — HTTP health checks, CloudWatch metric analysis, and integration tests. The `/checkout` endpoint returned HTTP 500 errors consistently across all integration test runs, while `/health` and `/products` endpoints remained healthy. The 5xx error rate exceeded the acceptable threshold, resulting in a **NO-GO** verdict and automatic rollback.

## Root Cause

The newly deployed Lambda version introduced a broken integration with `cart_service.get_cart()` inside the checkout handler. The function call failed at runtime due to an incompatible interface change in the cart service, causing unhandled exceptions that propagated as HTTP 500 responses on the `POST /checkout` endpoint.

## Auto-Remediation

Aziron detected the failure and automatically rolled back the Lambda alias `live` to the stable version.
Post-rollback verification: PASSED — all endpoints restored to 200 OK.

## Required Fix Before Re-Deploying

- Fix the cart_service.get_cart() integration in the checkout handler
- Ensure all 3 integration tests pass locally before deploying
- Deploy with: ./infra/deploy.sh deploy

---
*Auto-generated by Aziron Post-Deployment Validation Flow*
