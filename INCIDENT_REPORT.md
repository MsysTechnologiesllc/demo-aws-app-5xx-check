# Incident Report — Auto-Rollback Executed

**Date:** 2026-06-02T05:22:21.205Z
**Alarm:** demo-5xx-ApiGw5xxAlarm

## What Happened

A deployment was pushed to the Lambda function behind the API Gateway. Shortly after the deployment, the CloudWatch alarm `demo-5xx-ApiGw5xxAlarm` fired, indicating a spike in 5xx errors on the API Gateway stage. The Aziron Post-Deployment Validation Agent was triggered and performed HTTP health checks, CloudWatch metric analysis, and integration tests. The `/checkout` endpoint was found to be returning HTTP 500 errors consistently, while `/health` and `/products` remained healthy. Integration tests confirmed a failure in the checkout flow.

## Root Cause

The newly deployed Lambda version introduced a broken integration with `cart_service.get_cart()` inside the checkout handler. The function call failed at runtime due to an incompatible interface change, causing unhandled exceptions and 500 responses on all `POST /checkout` requests.

## Auto-Remediation

Aziron detected the failure and automatically rolled back the Lambda alias `live` to the stable version.
Post-rollback verification: PASSED — all endpoints restored to 200 OK.

## Required Fix Before Re-Deploying

- Fix the cart_service.get_cart() integration in the checkout handler
- Ensure all 3 integration tests pass locally before deploying
- Deploy with: ./infra/deploy.sh deploy

---
*Auto-generated by Aziron Post-Deployment Validation Flow*
