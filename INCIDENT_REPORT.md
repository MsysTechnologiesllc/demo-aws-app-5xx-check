# Incident Report — Auto-Rollback Executed

**Date:** 2026-05-15T10:15:26.433Z
**Alarm:** demo-5xx-ApiGw5xxAlarm

## What Happened

A deployment was triggered and the CloudWatch alarm `demo-5xx-ApiGw5xxAlarm` fired shortly after, indicating elevated 5xx error rates on the API Gateway. The Aziron Post-Deployment Validation Agent was invoked to perform health checks, fetch CloudWatch metrics, and run integration tests. The validation results returned a NO-GO verdict due to failing endpoint checks and a non-zero 5xx error count, confirming the deployment introduced a regression.

## Root Cause

The root cause was identified as a broken integration between the checkout handler and the `cart_service.get_cart()` function. The newly deployed Lambda version introduced an incompatible call to `cart_service.get_cart()`, causing the `/checkout` endpoint to return HTTP 500 errors. All other endpoints remained healthy, isolating the fault to the checkout integration path.

## Auto-Remediation

Aziron detected the failure and automatically rolled back the Lambda alias `live` to the stable version.
Post-rollback verification: PASSED — all endpoints restored to 200 OK.

## Required Fix Before Re-Deploying

- Fix the cart_service.get_cart() integration in the checkout handler
- Ensure all 3 integration tests pass locally before deploying
- Deploy with: ./infra/deploy.sh deploy

---
*Auto-generated by Aziron Post-Deployment Validation Flow*
