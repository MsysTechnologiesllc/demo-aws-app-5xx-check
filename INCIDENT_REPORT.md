# Incident Report — Auto-Rollback Executed

**Date:** 2026-06-29T12:40:30.926Z
**Alarm:** demo-5xx-ApiGw5xxAlarm

## What Happened

A CloudWatch 5xx alarm fired shortly after a new Lambda deployment was promoted to the `live` alias. Automated post-deployment validation detected HTTP 5xx responses on one or more API Gateway endpoints. Health checks, CloudWatch metric analysis, and integration tests were executed by the Aziron Post-Deployment Validation Agent, confirming the deployment was unhealthy and a rollback was required.

## Root Cause

The newly deployed Lambda version introduced a regression in the `/checkout` endpoint. The handler attempted to call `cart_service.get_cart()`, which failed due to an incompatible integration — causing unhandled exceptions and HTTP 500 responses to be returned to callers. All other endpoints remained healthy; the failure was isolated to the checkout flow.

## Auto-Remediation

Aziron detected the failure and automatically rolled back the Lambda alias `live` to the stable version.
Post-rollback verification: PASSED — all endpoints restored to 200 OK.

## Required Fix Before Re-Deploying

- Fix the cart_service.get_cart() integration in the checkout handler
- Ensure all 3 integration tests pass locally before deploying
- Deploy with: ./infra/deploy.sh deploy

---
*Auto-generated by Aziron Post-Deployment Validation Flow*
