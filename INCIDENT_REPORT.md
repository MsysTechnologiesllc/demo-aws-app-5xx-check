# Incident Report — Auto-Rollback Executed

**Date:** 2026-05-07T23:49:00.326Z
**Alarm:** demo-5xx-ApiGw5xxAlarm

## What Happened

A deployment was pushed to the Lambda function behind the API Gateway. Shortly after the deployment, the CloudWatch alarm `demo-5xx-ApiGw5xxAlarm` fired, indicating a surge in HTTP 5xx errors on the API Gateway stage. The Aziron Post-Deployment Validation Agent was triggered and performed automated health checks, CloudWatch metric analysis, and integration tests. The results confirmed that the `/checkout` endpoint was returning 500 Internal Server Error responses, while `/health` and `/cart` endpoints remained healthy.

## Root Cause

The newly deployed Lambda version introduced a broken integration with `cart_service.get_cart()` inside the checkout handler. The function call failed at runtime due to an incompatible interface change, causing unhandled exceptions and 500 responses on all requests routed to the `/checkout` endpoint.

## Auto-Remediation

Aziron detected the failure and automatically rolled back the Lambda alias `live` to the stable version.
Post-rollback verification: PASSED — all endpoints restored to 200 OK.

## Required Fix Before Re-Deploying

- Fix the cart_service.get_cart() integration in the checkout handler
- Ensure all 3 integration tests pass locally before deploying
- Deploy with: ./infra/deploy.sh deploy

---
*Auto-generated by Aziron Post-Deployment Validation Flow*
