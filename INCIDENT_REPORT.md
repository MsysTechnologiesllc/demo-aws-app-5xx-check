# Incident Report — Auto-Rollback Executed

**Date:** 2026-06-04T16:08:25.196Z
**Alarm:** demo-5xx-ApiGw5xxAlarm

## What Happened

A deployment triggered the CloudWatch alarm `demo-5xx-ApiGw5xxAlarm` due to elevated 5xx error rates on the API Gateway. Post-deployment health checks and integration tests confirmed that one or more endpoints were returning 5xx responses, indicating a broken deployment. The Aziron Post-Deployment Validation Agent detected the failure, issued a NO-GO verdict, and automatically initiated a rollback of the Lambda alias `live` to the last known stable version.

## Root Cause

The root cause was identified as a broken integration between the checkout handler and the `cart_service.get_cart()` function. The newly deployed Lambda version introduced a regression in the checkout endpoint (`POST /checkout`), causing it to return HTTP 500 errors. The `cart_service.get_cart()` call failed at runtime, propagating an unhandled exception through the checkout handler and resulting in 5xx responses visible in CloudWatch metrics.

## Auto-Remediation

Aziron detected the failure and automatically rolled back the Lambda alias `live` to the stable version.
Post-rollback verification: PASSED — all endpoints restored to 200 OK.

## Required Fix Before Re-Deploying

- Fix the cart_service.get_cart() integration in the checkout handler
- Ensure all 3 integration tests pass locally before deploying
- Deploy with: ./infra/deploy.sh deploy

---
*Auto-generated by Aziron Post-Deployment Validation Flow*
