# Incident Report — Auto-Rollback Executed

**Date:** 2026-06-17T20:22:46.339Z
**Alarm:** demo-5xx-ApiGw5xxAlarm

## What Happened

A CloudWatch 5xx alarm fired following a Lambda deployment. Aziron Post-Deployment Validation Agent was invoked and performed HTTP health checks, CloudWatch metric analysis, and integration tests against all API Gateway endpoints. The validation detected elevated 5xx error rates and failing integration tests, triggering an automatic NO-GO verdict and rollback procedure.

## Root Cause

The deployed Lambda version introduced a broken integration with `cart_service.get_cart()` inside the checkout handler. The checkout endpoint (`/checkout`) began returning HTTP 500 responses due to an unhandled exception when attempting to retrieve cart data, causing the API Gateway 5xx alarm to fire.

## Auto-Remediation

Aziron detected the failure and automatically rolled back the Lambda alias `live` to the stable version.
Post-rollback verification: PASSED — all endpoints restored to 200 OK.

## Required Fix Before Re-Deploying

- Fix the `cart_service.get_cart()` integration in the checkout handler
- Ensure all 3 integration tests pass locally before deploying
- Deploy with: `./infra/deploy.sh deploy`

---
*Auto-generated by Aziron Post-Deployment Validation Flow*
