# Incident Report — Auto-Rollback Executed

**Date:** 2026-05-21T21:15:05.086Z
**Alarm:** demo-5xx-ApiGw5xxAlarm

## What Happened

A deployment triggered a CloudWatch 5xx alarm on the API Gateway. Post-deployment validation detected elevated 5xx error rates across monitored endpoints. Health checks and integration tests confirmed that the `/checkout` endpoint was returning HTTP 500 errors, while `/health` and `/products` remained operational. The failure was isolated to the newly deployed Lambda version.

## Root Cause

The deployed Lambda version introduced a broken integration with `cart_service.get_cart()` inside the checkout handler. The function call failed at runtime due to an incompatible interface change, causing unhandled exceptions and 500 responses on all `POST /checkout` requests.

## Auto-Remediation

Aziron detected the failure and automatically rolled back the Lambda alias `live` to the stable version.
Post-rollback verification: PASSED — all endpoints restored to 200 OK.

## Required Fix Before Re-Deploying

- Fix the cart_service.get_cart() integration in the checkout handler
- Ensure all 3 integration tests pass locally before deploying
- Deploy with: ./infra/deploy.sh deploy

---
*Auto-generated by Aziron Post-Deployment Validation Flow*
