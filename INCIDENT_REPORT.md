# Incident Report — Auto-Rollback Executed

**Date:** 2026-06-18T10:59:46.432Z
**Alarm:** demo-5xx-ApiGw5xxAlarm

## What Happened

A CloudWatch 5xx alarm fired following a new Lambda deployment. Aziron Post-Deployment Validation Agent was invoked and performed HTTP health checks against all API Gateway endpoints, fetched CloudWatch metrics to quantify the 5xx error rate, and executed the full integration test suite. The `/checkout` endpoint returned HTTP 500 responses consistently, while `/health` and `/products` endpoints remained healthy. CloudWatch metrics confirmed an elevated 5xx error rate originating from the newly deployed Lambda version. Integration tests corroborated the failure, with the `test_checkout` case failing due to an unhandled exception in the checkout handler.

## Root Cause

The `cart_service.get_cart()` function integration in the checkout handler is broken in the deployed version. The new deployment introduced a breaking change in the way the checkout Lambda handler calls `cart_service.get_cart()`, resulting in an unhandled exception and HTTP 500 responses on every request to the `/checkout` endpoint.

## Auto-Remediation

Aziron detected the failure and automatically rolled back the Lambda alias `live` to the stable version.
Post-rollback verification: PASSED — all endpoints restored to 200 OK.

## Required Fix Before Re-Deploying

- Fix the `cart_service.get_cart()` integration in the checkout handler
- Ensure all 3 integration tests pass locally before deploying
- Deploy with: `./infra/deploy.sh deploy`

---
*Auto-generated by Aziron Post-Deployment Validation Flow*
