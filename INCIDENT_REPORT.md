# Incident Report — Auto-Rollback Executed

**Date:** 2026-06-25T08:00:46.568Z
**Alarm:** demo-5xx-ApiGw5xxAlarm

## What Happened

A CloudWatch 5xx alarm fired shortly after a new Lambda deployment was promoted to the `live` alias. Aziron Post-Deployment Validation Agent was automatically invoked to perform health checks, fetch CloudWatch metrics, and execute integration tests against all API Gateway endpoints. The validation run detected HTTP 5xx responses on the `/checkout` endpoint, confirmed by elevated `5XXError` metric counts in CloudWatch and a failing integration test targeting the checkout flow.

## Root Cause

The newly deployed Lambda version introduced a broken integration with `cart_service.get_cart()` inside the checkout handler. The function raised an unhandled exception when attempting to retrieve cart data, causing the Lambda to return a 500 Internal Server Error for all requests routed to `POST /checkout`. All other endpoints (`/health`, `/products`) remained healthy throughout the incident.

## Auto-Remediation

Aziron detected the failure and automatically rolled back the Lambda alias `live` to the stable version.
Post-rollback verification: PASSED — all endpoints restored to 200 OK.

## Required Fix Before Re-Deploying

- Fix the `cart_service.get_cart()` integration in the checkout handler
- Ensure all 3 integration tests pass locally before deploying
- Deploy with: `./infra/deploy.sh deploy`

---
*Auto-generated by Aziron Post-Deployment Validation Flow*
