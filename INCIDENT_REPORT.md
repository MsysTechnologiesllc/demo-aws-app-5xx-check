# Incident Report — Auto-Rollback Executed

**Date:** 2026-06-02T10:11:26.831Z
**Alarm:** demo-5xx-ApiGw5xxAlarm

## What Happened

A deployment triggered a CloudWatch 5xx alarm (`demo-5xx-ApiGw5xxAlarm`) shortly after release. Aziron Post-Deployment Validation Agent was invoked and performed HTTP health checks, CloudWatch metric analysis, and integration tests. The `/checkout` endpoint was found to be returning HTTP 500 errors consistently, while `/health` and `/cart` endpoints remained healthy. Integration tests confirmed a failure in the checkout flow, resulting in a **NO-GO** verdict.

## Root Cause

The newly deployed Lambda version introduced a broken integration with `cart_service.get_cart()` inside the checkout handler. The function call raised an unhandled exception, causing the `/checkout` endpoint to return 500 Internal Server Error responses. This was not caught by pre-deployment unit tests because the cart service mock was not updated to reflect the new interface contract.

## Auto-Remediation

Aziron detected the failure and automatically rolled back the Lambda alias `live` to the stable version.
Post-rollback verification: PASSED — all endpoints restored to 200 OK.

## Required Fix Before Re-Deploying

- Fix the `cart_service.get_cart()` integration in the checkout handler
- Ensure all 3 integration tests pass locally before deploying
- Deploy with: `./infra/deploy.sh deploy`

---
*Auto-generated by Aziron Post-Deployment Validation Flow*
