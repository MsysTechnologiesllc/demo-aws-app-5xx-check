# Incident Report — Auto-Rollback Executed

**Date:** 2026-06-05T19:59:18.347Z
**Alarm:** demo-5xx-ApiGw5xxAlarm

## What Happened

A deployment to the Lambda function triggered a CloudWatch 5xx alarm on the API Gateway stage. Post-deployment validation detected elevated HTTP 500 error rates across one or more endpoints. The `/checkout` endpoint was identified as the failing route, returning 500 Internal Server Error responses during integration testing, while `/health` and `/products` endpoints remained healthy.

## Root Cause

The root cause was identified as a broken integration between the checkout handler and the `cart_service.get_cart()` function. The newly deployed Lambda version introduced a regression in the cart service call within the checkout flow, causing unhandled exceptions that propagated as 500 responses through API Gateway.

## Auto-Remediation

Aziron detected the failure and automatically rolled back the Lambda alias `live` to the stable version.
Post-rollback verification: PASSED — all endpoints restored to 200 OK.

## Required Fix Before Re-Deploying

- Fix the cart_service.get_cart() integration in the checkout handler
- Ensure all 3 integration tests pass locally before deploying
- Deploy with: ./infra/deploy.sh deploy

---
*Auto-generated by Aziron Post-Deployment Validation Flow*
