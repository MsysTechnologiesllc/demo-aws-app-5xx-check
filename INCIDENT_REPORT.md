# Incident Report — Auto-Rollback Executed

**Date:** 2026-05-21T19:09:20.303Z
**Alarm:** demo-5xx-ApiGw5xxAlarm

## What Happened

A CloudWatch 5xx alarm fired shortly after a new Lambda deployment was promoted to the `live` alias. Aziron Post-Deployment Validation Agent was automatically invoked to assess service health. HTTP health checks, CloudWatch metric analysis, and integration tests were executed against all API Gateway endpoints. The `/checkout` endpoint returned HTTP 500 responses consistently across all integration test runs, while `/health` and `/products` endpoints remained healthy. The 5xx error rate exceeded the acceptable threshold, triggering an automatic NO-GO verdict and rollback.

## Root Cause

The root cause was identified as a broken integration between the checkout Lambda handler and the `cart_service.get_cart()` function. The newly deployed version introduced a call to `cart_service.get_cart()` that failed at runtime — likely due to an incompatible interface change, missing environment variable, or unhandled exception within the cart service client. This caused all requests to `POST /checkout` to return HTTP 500, generating the 5xx alarm.

## Auto-Remediation

Aziron detected the failure and automatically rolled back the Lambda alias `live` to the stable version.
Post-rollback verification: PASSED — all endpoints restored to 200 OK.

## Required Fix Before Re-Deploying

- Fix the `cart_service.get_cart()` integration in the checkout handler
- Ensure all 3 integration tests pass locally before deploying
- Deploy with: `./infra/deploy.sh deploy`

---
*Auto-generated by Aziron Post-Deployment Validation Flow*
