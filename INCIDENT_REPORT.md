# Incident Report — Auto-Rollback Executed

**Date:** 2026-06-02T05:00:21.252Z
**Alarm:** demo-5xx-ApiGw5xxAlarm

## What Happened

A deployment was pushed that triggered the CloudWatch alarm `demo-5xx-ApiGw5xxAlarm`. Post-deployment validation detected elevated 5xx error rates on the API Gateway endpoints. Health checks and integration tests confirmed that one or more endpoints were returning non-2xx responses, indicating a broken deployment. Aziron's Post-Deployment Validation Agent was automatically invoked to assess the failure and execute remediation.

## Root Cause

The root cause was identified as a broken integration between the checkout handler and the `cart_service.get_cart()` function. The newly deployed Lambda version introduced a regression in the `/checkout` endpoint, causing it to return 5xx errors when attempting to retrieve cart data. This failure propagated through the API Gateway, triggering the CloudWatch alarm.

## Auto-Remediation

Aziron detected the failure and automatically rolled back the Lambda alias `live` to the stable version.
Post-rollback verification: PASSED — all endpoints restored to 200 OK.

## Required Fix Before Re-Deploying

- Fix the cart_service.get_cart() integration in the checkout handler
- Ensure all 3 integration tests pass locally before deploying
- Deploy with: ./infra/deploy.sh deploy

---
*Auto-generated by Aziron Post-Deployment Validation Flow*
