# Incident Report — Auto-Rollback Executed

**Date:** 2026-06-02T17:59:33.170Z
**Alarm:** demo-5xx-ApiGw5xxAlarm

## What Happened

A deployment was pushed to the Lambda function and promoted to the `live` alias. Shortly after promotion, the CloudWatch alarm `demo-5xx-ApiGw5xxAlarm` fired, indicating a surge in HTTP 5xx errors on the API Gateway. The Aziron Post-Deployment Validation Agent was triggered and performed health checks, CloudWatch metric analysis, and integration tests. The results confirmed that the `/checkout` endpoint was returning 500 Internal Server Error responses, while `/health` and `/products` remained healthy. The overall verdict was **NO-GO**.

## Root Cause

The newly deployed Lambda version introduced a broken integration with `cart_service.get_cart()` inside the checkout handler. The function call failed at runtime, causing unhandled exceptions that propagated as 500 responses through API Gateway. The defect was not caught prior to deployment because the integration test suite was not executed against the staging environment before the alias was promoted.

## Auto-Remediation

Aziron detected the failure and automatically rolled back the Lambda alias `live` to the stable version.
Post-rollback verification: PASSED — all endpoints restored to 200 OK.

## Required Fix Before Re-Deploying

- Fix the cart_service.get_cart() integration in the checkout handler
- Ensure all 3 integration tests pass locally before deploying
- Deploy with: ./infra/deploy.sh deploy

---
*Auto-generated by Aziron Post-Deployment Validation Flow*
