# Incident Report — Auto-Rollback Executed

**Date:** 2026-05-15T08:59:56.132Z
**Alarm:** demo-5xx-ApiGw5xxAlarm

## What Happened

A CloudWatch 5xx alarm fired shortly after a new Lambda deployment was promoted to the `live` alias. Aziron Post-Deployment Validation Agent was triggered and executed a full validation sweep: HTTP health checks, CloudWatch metric analysis, and integration tests. The `/checkout` endpoint returned HTTP 500 on every request, while `/health` and `/products` remained healthy. Integration tests confirmed a broken dependency in the checkout handler, resulting in a **NO-GO** verdict.

## Root Cause

The `checkout` Lambda handler attempted to call `cart_service.get_cart()`, which was not available in the newly deployed version due to a missing or misconfigured integration with the cart microservice. This caused all `POST /checkout` requests to throw an unhandled exception, producing HTTP 500 responses at the API Gateway layer.

## Auto-Remediation

Aziron detected the failure and automatically rolled back the Lambda alias `live` to the stable version.
Post-rollback verification: PASSED — all endpoints restored to 200 OK.

## Required Fix Before Re-Deploying

- Fix the `cart_service.get_cart()` integration in the checkout handler
- Ensure all 3 integration tests pass locally before deploying
- Deploy with: `./infra/deploy.sh deploy`

---
*Auto-generated by Aziron Post-Deployment Validation Flow*
