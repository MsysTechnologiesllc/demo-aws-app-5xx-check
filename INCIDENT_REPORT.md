# Incident Report — Auto-Rollback Executed

**Date:** 2026-05-15T11:51:44.166Z
**Alarm:** demo-5xx-ApiGw5xxAlarm

## What Happened

A CloudWatch 5xx alarm fired shortly after a new Lambda deployment was promoted to the `live` alias. Post-deployment validation detected elevated HTTP 5xx error rates on the API Gateway endpoints. Health checks against the `/checkout` endpoint returned non-2xx responses, and integration tests confirmed that the checkout flow was broken, triggering an automatic NO-GO verdict and rollback.

## Root Cause

The `cart_service.get_cart()` function call inside the checkout Lambda handler raised an unhandled exception due to an incompatible interface change introduced in the new deployment. The handler attempted to call `get_cart()` with a revised parameter signature that the underlying cart service did not yet support, causing a 500 Internal Server Error on every request routed through the `/checkout` endpoint.

## Auto-Remediation

Aziron detected the failure and automatically rolled back the Lambda alias `live` to the stable version.
Post-rollback verification: PASSED — all endpoints restored to 200 OK.

## Required Fix Before Re-Deploying

- Fix the `cart_service.get_cart()` integration in the checkout handler
- Ensure all 3 integration tests pass locally before deploying
- Deploy with: `./infra/deploy.sh deploy`

---
*Auto-generated by Aziron Post-Deployment Validation Flow*
