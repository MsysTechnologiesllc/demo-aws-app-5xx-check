# Incident Report — Auto-Rollback Executed

**Date:** 2026-06-17T16:18:46.284Z
**Alarm:** demo-5xx-ApiGw5xxAlarm

## What Happened

A CloudWatch 5xx alarm fired shortly after the latest Lambda deployment was promoted to the `live` alias. Post-deployment validation detected elevated HTTP 500 error rates on the `/checkout` endpoint. Health checks against the API Gateway stage confirmed that the `/checkout` route was returning 500 responses, while `/health` and `/products` remained healthy. Integration tests further isolated the failure to the checkout handler, which was unable to complete requests due to a broken dependency on `cart_service.get_cart()`.

## Root Cause

The newly deployed Lambda version introduced a regression in the checkout handler's integration with `cart_service.get_cart()`. The function call failed at runtime, causing unhandled exceptions that propagated as HTTP 500 responses to API Gateway consumers. The root cause is an incompatible interface change in `cart_service.get_cart()` that was not caught by pre-deployment unit tests.

## Auto-Remediation

Aziron detected the failure and automatically rolled back the Lambda alias `live` to the stable version.
Post-rollback verification: PASSED — all endpoints restored to 200 OK.

## Required Fix Before Re-Deploying

- Fix the cart_service.get_cart() integration in the checkout handler
- Ensure all 3 integration tests pass locally before deploying
- Deploy with: ./infra/deploy.sh deploy

---
*Auto-generated by Aziron Post-Deployment Validation Flow*
